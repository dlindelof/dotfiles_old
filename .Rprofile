options(defaultPackages = c(getOption("defaultPackages"), "lattice"))

beamer.theme <- list(add.line         = list(lwd = 5),
                     axis.line        = list(lwd = 5),
                     axis.text        = list(cex = 2.5),
                     par.xlab.text    = list(cex = 3),
                     par.ylab.text    = list(cex = 3),
                     plot.symbol      = list(pch = 16, cex = 7),
                     plot.line        = list(lwd = 3),
                     reference.line   = list(lwd = 3),
                     superpose.symbol = list(pch = 16, cex = 7))
